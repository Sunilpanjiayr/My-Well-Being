# Root .env file for production (Firebase + Railway)

# Server Configuration
PORT=8080
NODE_ENV=production

# CORS Configuration
ALLOWED_ORIGINS=https://my-wellbeing-new01-production.up.railway.app

# Firebase Admin SDK Configuration
GOOGLE_APPLICATION_CREDENTIALS=src/forum_backend/config/firebase-service-account.json

# JWT Configuration
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=7d 