# Backend .env file for production (Firebase + Railway)

# Server Configuration
PORT=8080
NODE_ENV=production

# Firebase Admin SDK Configuration
GOOGLE_APPLICATION_CREDENTIALS=../config/firebase-service-account.json

# CORS Configuration
ALLOWED_ORIGINS=https://my-wellbeing-new01-production.up.railway.app